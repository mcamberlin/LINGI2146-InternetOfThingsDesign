1. Open a new terminal and RUN: contiker cooja 
2. Load project_simu.csc
3. Start simulation
    (
        Execute the next command to retrieve the name (<container-name>) of the current docker container running contiki :
            docker ps
        Execute the next command to search the ip address (<container-ip> : 172.17.0.1) of this container:
            docker container inspect <container-name>
    )   
4. Open a new terminal and RUN: cd contiki-ng/LINGI2146-InternetOfThingsDesign/ && python3 init.py
5. Open a new terminal and RUN: gcc -Wall -Werror -c test datagram.c datagram.h server.c && ./test
6. echo blablabla | nc -u bbbb::206:6:6:6 3000 
